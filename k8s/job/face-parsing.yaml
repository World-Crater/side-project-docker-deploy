apiVersion: batch/v1
kind: Job
metadata:
  name: face-parsing-job
spec:
  template:
    spec:
      containers:
      - name: face-parsing
        imagePullPolicy: Always
        image: superj80820/face-parsing
        env:
        - name: FACE_SERVICE
          valueFrom:
            configMapKeyRef:
              name: global-configmap
              key: service.face
        - name: RESOURCE_URL_PAGE
          value: "320"
      restartPolicy: Never
  backoffLimit: 1